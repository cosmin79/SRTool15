int x;
int y;

int main() {
  int a;
  int b;
  a = 100;
  b = 1000;
  if (a > b && a + b < x) {
    assert 1 > 2;
    a = a * 1000;
    b = b * y;
    if (a * b == y) {
      assert a == 100;
      y  = y * y;
    } else {
      assert x == 872;
      y = x * y;
    }
  } else {
    y = y + x * x;
  }
  return a + b * x;
}
